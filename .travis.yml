language: go
go:
  - 1.12.x
  - master
install: true

jobs:
  include:
    - stage: calculator
      before_script: cd src/calculator
      script:
        - go build
        - go test -v

    - stage: api
      before_script: cd src/api
      script:
        - go build
        - go test -v
